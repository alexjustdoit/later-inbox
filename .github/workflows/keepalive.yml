name: Keep app alive

on:
  schedule:
    # Ping every 5 hours to prevent SCC sleep
    - cron: "0 */5 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: |
          curl -s -o /dev/null -w "%{http_code}" ${{ secrets.APP_URL }}
